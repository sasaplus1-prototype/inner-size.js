{
  "name": "inner-size",
  "version": "0.1.2",
  "author": "sasa+1 <sasaplus1@gmail.com>",
  "description": "cross browser innerWidth/innerHeight",
  "main": "./index.js",
  "license": "MIT",
  "readmeFilename": "README.md",
  "repository": {
    "type": "git",
    "url": "https://github.com/sasaplus1-prototype/inner-size.js.git"
  },
  "scripts": {
    "build": "npm run compile && npm run minify",
    "compile": "npm run webpack -- --output-file inner-size.js",
    "minify": "npm run webpack -- --optimize-minimize --output-file inner-size.min.js",
    "webpack": "webpack --colors --display-error-details --progress"
  },
  "devDependencies": {
    "webpack": "^1.13.1"
  }
}
